apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "telemongo.fullname" . }}-test-connection"
  labels:
    {{- include "telemongo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "telemongo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
